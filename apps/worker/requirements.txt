httpx==0.28.1
redis==5.2.1
pydantic==2.10.3
pydantic-settings==2.6.1
numpy==2.1.3
soundfile==0.12.1
# Faster-Whisper (CPU by default; GPU if torch+cuda present)
faster-whisper==1.1.0
# Piper TTS via subprocess binary preferred; python fallback:
piper-tts==1.2.0
